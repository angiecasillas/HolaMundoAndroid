<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="botonInicio" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="android:background">transparent</item>
    <item name="android:text">@string/txtbutton_app</item>
    <item name="android:textColor">@color/abc_background_cache_hint_selector_material_light</item>
    <item name="android:shadowColor">@color/abc_background_cache_hint_selector_material_light</item>
    </style>

    <style name="fondoPantallas" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@drawable/marcianito_miprimer_proyecto_android___copia</item>

    </style>

    <string name="entrar">Entrar</string>
</resources>
